[project]
name = "leansim"
version = "0.1.0"
description = "Launcher and interactive analysis for leansim Shadow runs"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
analyze = [
    "plotly>=5.0",
]

[tool.uv]
package = false
